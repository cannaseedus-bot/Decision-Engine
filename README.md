# Decision-Engine

Canonical grammar artifact:

- `ASX-Decision-Verification.ebnf` — frozen structural grammar for ASX decision, conformance, trace, shard verdict, and badge record syntax.

Canonical CLI package:

- `asx-decision/` — PowerShell-first Decision Engine CLI with SCXQ encoders, decision collapse, conformance runner, trace attestation, and badge generation.
